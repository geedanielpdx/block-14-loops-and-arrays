// 1. Create an 'originalArray' with some values (ie, const originalArray = [1, -1, 2, -3, 5, -8, 13])
// 2. Use the 'reverse()' method on 'originalArray' to reverse its order
// 3. Log the 'reversedArray' to the console

const originalArray = [1, -1, 2, -3, 5, -8, 13]; //Enter numbers, separated by commas between the brackets
const reverseArray = originalArray.reverse();

console.log(reverseArray) // Output: [Numbers in reverse order]
